#TODO: Replace spring.datasource.* after contenerization
server:
  port: 9099
spring:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  sql:
    init:
      platform: postgres
  datasource:
    url: jdbc:postgresql://localhost:16832/payments_mcsv
    username: postgres
    password: root
  rabbitmq:
    host: localhost
    port: 5672
    password: guest
    username: guest