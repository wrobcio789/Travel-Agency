#TODO: Replace spring.datasource.* after contenerization
spring:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  sql:
    init:
      platform: postgres
  datasource:
    url: jdbc:postgresql://host.docker.internal:5432/RSWW_175782_payments
    username: postgres
    password: student
  rabbitmq:
    host: rabbitmq
    port: 5672
    password: guest
    username: guest